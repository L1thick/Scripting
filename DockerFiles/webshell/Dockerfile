# Use the official PHP image with Apache
FROM php:7.4-apache

# Copy the contents of the src directory into the web server's root directory
COPY src/ /var/www/html/

# Expose port 80 to make the web server accessible
EXPOSE 80
